# 瓷磚地圖編輯器 (Tilemap Editor) - 使用說明

## 🎨 功能概述

瓷磚地圖編輯器是一個像 **Unity Tilemap** 一樣的地圖編輯工具，允許你：

- 📥 **導入瓷磚圖像** - 從 PNG、JPG 等圖像文件導入作為瓷磚集合
- 🎨 **繪製地圖** - 使用繪製工具在網格上繪製瓷磚
- 🗑️ **擦除瓷磚** - 使用橡皮擦工具移除不需要的瓷磚
- 💾 **保存/加載** - 將地圖保存為二進制文件，以便後續加載
- ↶ **撤銷功能** - 支持最多 50 步的撤銷操作

## 🚀 快速開始

### 運行方式

**方式 1：直接運行批處理檔案**
```batch
啟動瓷磚地圖編輯器.bat
```

**方式 2：使用 Maven**
```bash
mvn clean compile
mvn exec:java -Dexec.mainClass="TilemapEditorGUI"
```

**方式 3：手動編譯並運行**
```bash
javac -d target/classes *.java
java -cp target/classes TilemapEditorGUI
```

## 🎯 使用步驟

### 第 1 步：導入瓷磚圖像

1. 在左側面板點擊 **「📥 導入瓷磚圖像」** 按鈕
2. 選擇你的瓷磚圖像文件（PNG、JPG、BMP 等）
3. 系統會自動計算瓷磚數量和尺寸
4. 調整 **瓷磚大小** 滑塊以匹配你的瓷磚尺寸（默認 32x32）

### 第 2 步：選擇瓷磚

1. 在左側的瓷磚集合區域 **點擊選擇** 你想使用的瓷磚
2. 選中的瓷磚會用 **黃色邊框** 標記
3. 顯示 "已選瓷磚: #X" 表示選擇成功

### 第 3 步：在地圖上繪製

1. 在中央的地圖編輯區域 **左鍵點擊** 放置瓷磚
2. **左鍵拖曳** 可以連續繪製多個瓷磚
3. 網格會顯示各瓷磚的邊界

### 第 4 步：擦除瓷磚

1. 在右側工具欄選擇 **「橡皮擦」** 工具
2. **左鍵點擊** 要刪除的瓷磚
3. 或按 **E 鍵** 快速切換到橡皮擦

### 第 5 步：保存地圖

1. 點擊 **「💾 儲存地圖」** 按鈕
2. 地圖會保存為 `tilemap_config.dat` 文件
3. 下次啟動時可以點擊 **「📂 載入地圖」** 恢復

## ⌨️ 快速鍵

| 快速鍵 | 功能 |
|--------|------|
| **D** | 快速切換到繪製工具 |
| **E** | 快速切換到橡皮擦 |
| **Delete** | 擦除選中的瓷磚 |
| **Ctrl+Z** | 撤銷上一步操作 |

## 🎮 工具說明

### 繪製工具 (DRAW)
- 選擇一個瓷磚後，點擊地圖位置來放置瓷磚
- 支持拖曳連續繪製

### 橡皮擦工具 (ERASE)
- 點擊地圖上的瓷磚來刪除
- 支持拖曳連續擦除

### 選擇工具 (SELECT)
- 用於查看地圖信息（暫未完全實現）

## 📐 坐標系統

地圖使用兩套坐標系統：

| 坐標類型 | 範圍 | 說明 |
|---------|------|------|
| **瓷磚座標** | 0-80 (寬), 0-18 (高) | 編輯器中使用 |
| **遊戲座標** | 0-4800 (寬), 0-1080 (高) | 與遊戲內相同 |

當滑鼠移動時，狀態欄會顯示兩種座標格式：
```
座標: (100, 200) [瓷磚: 2, 3]
```

## 📁 文件說明

### 主要文件

| 文件名 | 說明 |
|--------|------|
| `TilemapEditorGUI.java` | 主編輯器界面 |
| `TilemapConfig.java` | 地圖數據管理 |
| `tilemap_config.dat` | 保存的地圖數據（二進制） |
| `啟動瓷磚地圖編輯器.bat` | Windows 啟動批處理 |

## 🛠️ 瓷磚尺寸設置

### 預設值
- **寬**: 32 像素
- **高**: 32 像素

### 調整方法
1. 在左側面板找到「瓷磚大小」
2. 根據你的瓷磚圖像尺寸調整
3. 例如：如果瓷磚是 64x64，設置寬高為 64

### 計算方式
```
瓷磚數 = 圖像寬度 ÷ 瓷磚寬度
```

## 💡 最佳實踐

### 瓷磚集合準備
1. 確保所有瓷磚大小相同
2. 瓷磚應按行排列（如 8x4 = 32 個瓷磚）
3. 推薦尺寸：32x32、64x64、16x16

### 地圖編輯
1. 先導入瓷磚集合，再開始繪製
2. 經常保存地圖（Ctrl+S 或點擊保存按鈕）
3. 利用撤銷功能（Ctrl+Z）修正錯誤

### 性能建議
- 避免頻繁的大量編輯
- 定期保存地圖
- 瓷磚圖像不超過 4096x4096

## 🔧 故障排除

### 問題：無法導入圖像
- 確保圖像格式為 PNG、JPG 或 BMP
- 檢查文件路徑中是否有特殊字符
- 確保有足夠的文件讀取權限

### 問題：瓷磚未正確顯示
- 驗證瓷磚尺寸設置是否正確
- 如果瓷磚被裁剪，嘗試調整瓷磚大小滑塊
- 確保圖像實際尺寸是瓷磚大小的整數倍

### 問題：地圖無法保存
- 確保有寫入權限
- 檢查磁盤空間是否充足
- 嘗試關閉其他打開的地圖編輯器實例

## 📊 編輯器統計

狀態欄顯示：
- 當前滑鼠位置（遊戲座標和瓷磚座標）
- 已選瓷磚 ID
- 最後操作的結果消息

## 🎓 高級技巧

### 快速繪製背景
1. 選擇草地/背景瓷磚
2. 點擊左上角並拖曳到右下角
3. 快速填充整個地圖

### 分層編輯（手動方式）
1. 先繪製底層瓷磚（背景）
2. 保存地圖
3. 加載地圖後繪製上層瓷磚（建築物、障礙物等）

## 📝 注意事項

1. **兼容性**：需要 Java 21 或更高版本
2. **分辨率**：推薦在 1920x1080 或更高分辨率下使用
3. **瓷磚限制**：最多支持 80x18 = 1440 個瓷磚
4. **撤銷深度**：最多保留 50 步撤銷歷史

## 🔄 版本信息

- **版本**: 1.0.0
- **Java 版本**: 21+
- **最後更新**: 2025-12-07

## 📞 技術支持

如有問題或建議，請檢查：
1. Java 版本是否為 21 或更高
2. 所有必需的 Java 類文件是否存在
3. 瓷磚圖像文件的有效性

---

**祝你編輯愉快！** 🎉
